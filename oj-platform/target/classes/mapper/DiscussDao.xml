<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheerful.oj.platform.dao.DiscussDao">

    <resultMap type="com.cheerful.oj.platform.entity.Discuss" id="DiscussMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="authorId" column="author_id" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="gmtCreate" column="gmt_create" jdbcType="TIMESTAMP"/>
        <result property="favorNum" column="favor_num" jdbcType="INTEGER"/>
        <result property="gmtModified" column="gmt_modified" jdbcType="TIMESTAMP"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into oj_ums.ums_discuss(author_id, content, gmt_create, favor_num, gmt_modified, title)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.authorId}, #{entity.content}, #{entity.gmtCreate}, #{entity.favorNum}, #{entity.gmtModified},
            #{entity.title})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into oj_ums.ums_discuss(author_id, content, gmt_create, favor_num, gmt_modified, title)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.authorId}, #{entity.content}, #{entity.gmtCreate}, #{entity.favorNum}, #{entity.gmtModified},
            #{entity.title})
        </foreach>
        on duplicate key update
        author_id = values(author_id) , content = values(content) , gmt_create = values(gmt_create) , favor_num =
        values(favor_num) , gmt_modified = values(gmt_modified) , title = values(title)
    </insert>

</mapper>

